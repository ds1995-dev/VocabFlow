<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Study Timezone
    |--------------------------------------------------------------------------
    |
    | 学習日（ストリークにおける「1日」）の区切りに使うタイムゾーン。
    | config/app.php の timezone は UTC のまま変更しない。あれを変えると
    | 既存の timestamps の解釈がまとめてずれてしまうため、
    | 「アプリの内部時刻」と「学習日の境界」を別の関心事として分けている。
    |
    */

    'timezone' => env('STUDY_TIMEZONE', 'Asia/Tokyo'),

];
