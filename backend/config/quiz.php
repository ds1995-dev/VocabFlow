<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Leitner Intervals
    |--------------------------------------------------------------------------
    |
    | 箱ごとの復習間隔（日）。配列の添字がそのまま word_reviews.box の値になる。
    | 正解すると1つ上の箱へ進み、誤答すると箱 0 に戻る。
    | 間隔をコードに直書きせずここに出しておくことで、学習ペースの調整が
    | このファイルの差し替えだけで済む。
    |
    */

    'intervals' => [1, 3, 7, 14, 30, 60],

    /*
    |--------------------------------------------------------------------------
    | Learned Box
    |--------------------------------------------------------------------------
    |
    | この箱以上に到達した単語は words.is_learned を true にする。
    | 上げるだけで、誤答しても false には戻さない。手動で付けた学習済みフラグを
    | クイズが勝手に剥がさないようにするための片方向ルール。
    |
    */

    'learned_box' => 3,

    /*
    |--------------------------------------------------------------------------
    | Question Settings
    |--------------------------------------------------------------------------
    |
    | choice_count は4択の「4」。誤答の選択肢はユーザー自身の他の単語から作るため、
    | 登録単語がこの数に満たないアカウントではクイズを組み立てられない。
    |
    */

    'choice_count' => 4,

    'default_question_count' => 10,

    'max_question_count' => 50,

];
